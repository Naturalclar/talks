import Code from "mdx-code";
import { Notes, Head } from "mdx-deck";
import { Avatar } from "@naturalclar/slides-components";
import { CodeSurferLayout, CodeSurferColumnLayout } from "code-surfer";
export { dracula as theme } from "code-surfer";

<Head>
  <Meta
    title="mdx-deck-code-surfer-v3"
    description="Showcase for mdx-deck and code-surfer v3"
    publishedAt={new Date("2019-08-29").toISOString()}
    host="https://code-surfer-v3.naturalclar.now.sh"
  />
</Head>

## Code Surfer v3

[WEB エンジニア勉強会 #14](https://web-engineer-meetup.connpass.com/event/141700/)
@naturalclar

<Notes>
  それでは、mdx-deckとcode-surfer v3
  について登壇させていただきます。Naturalclarです。
  この発表スライドは、先程、TwitterにHashtagをつけて投稿したので、そちらからも、確認できます。
</Notes>

---

## About Me

<Avatar />

<Profile />

<Notes>
  まずは自己紹介です。Jesseです。アメリカ人です。
  去年の9月に日本に引っ越してきました。 CureAppという会社で主にReact Native と
  TypeScript を扱った医療系のアプリを開発しています。
</Notes>

---

## mdx-deck とは

<img src={require("file-loader!./mdx-deck-slide.png")} width="50%" >

[mdx-deck 紹介スライド](https://takumon.github.io/mdx-deck-sample/)

<Notes>
  mdx-deckは簡単に言えば、markdownで簡単にスライドが作れるものです。
  このスライドもmdx-deckを使って作成しています。
  以前、この勉強会でmdx-deckについて登壇している資料がわかりやすいので、おすすめです。
  今回は、v3になって追加された機能について話します。
</Notes>

- 作者が一から作り直してるので Breaking Change しかない

---

## mdx-deck v3

- hooks api

---

## code-surfer v3

[code-surfer v3 PR](https://github.com/pomber/code-surfer/pull/58)

---

<CodeSurferLayout>

```js title="タイトルつけれる"
function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js 1:3 subtitle="字幕も今まで通りつけれる"
function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js subtitle="前のページとのDiffを取って、新しく追加されたコードを挿入"
function helloFactory() {
  return name => {
    console.log("hello " + name);
  };
}

function hello(name) {
  console.log("hello " + name);
}

hello("world");
```

```js subtitle="変更があった箇所は差し替えられる"
function helloFactory() {
  return name => {
    console.log("こんにちは " + name);
  };
}

function hello(name) {
  console.log("こんにちは " + name);
}

hello("world");
```

```js 1:7 subtitle="削除されたところは消える"
function helloFactory() {
  return name => {
    console.log("こんにちは " + name);
  };
}

hello("world");
```

</CodeSurferLayout>

---
