import Code from "mdx-code";
import { Notes, Head } from "mdx-deck";
import { Avatar, Meta, Profile } from "@naturalclar/slides-components";
import { CodeSurferLayout, CodeSurferColumnLayout } from "code-surfer";
export { vsDark as theme } from "code-surfer";
import "prismjs/components/prism-tsx";

<Head>
  <Meta
    title="react-with-typescript"
    description="Enter description here"
    publishedAt={"2019-09-04T05:13:32.814Z"}
    host="https://react-with-typescript.naturalclar.now.sh"
  />
</Head>

## React and Redux with TypeScript

[#reactjs_lt](https://informetis.connpass.com/event/142183/)
@naturalclar

<Notes></Notes>

---

## About Me

<Avatar />
<Profile />

<Notes>
  まずは自己紹介です。Jesseと申します。アメリカ人です。
  去年の9月に日本に引っ越してきました。 CureAppという会社で主にReact Native と
  TypeScript を扱った医療系のアプリを開発しています。
</Notes>

---

# react

---

## Class component

---

<CodeSurferLayout>

```tsx subtitle="Intro.tsx"
import React from "react";

type Props = {
  name: string;
  age: number;
};

type State = {
  hobby: string;
};

class Intro extends React.Component<Props, State> {}
```

</CodeSurferLayout>

---

## Function Component

---

<CodeSurferLayout>

```tsx subtitle="Intro.tsx"
import React from "react";

type Props = {
  name: string;
  age: number;
};

const Intro: React.FC<Props> = ({ name, age }: Props) => {
  return (
    <p>
      Hello! My name is {name}, I'm {age} years old.
    </p>
  );
};
```

</CodeSurferLayout>

---

## useState

---

<CodeSurferLayout>

```tsx subtitle="Intro.tsx"
import React from "react";

type Props = {
  name: string;
  age: number;
};

type;

const Intro: React.FC<Props> = ({ name, age }: Props) => {
  return (
    <p>
      Hello! My name is {name}, I'm {age} years old.
    </p>
  );
};
```

</CodeSurferLayout>

---

## useMemo

---

## useContext

---

# redux

---

ありがとうございました。
